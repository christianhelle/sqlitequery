name: Windows

on:
  push:
    paths-ignore:
      - '**/*'
      - '!.github/workflows/windows*.yml'
      - '!src/**/*.cpp'
      - '!src/**/*.h'
    branches:
      - 'master'
  pull_request:
    paths-ignore:
      - '**/*'
      - '!.github/workflows/windows*.yml'
      - '!src/**/*.cpp'
      - '!src/**/*.h'
    branches:
      - '*'

jobs:
  build:
    uses: ./.github/workflows/windows-template.yml
    with:
      version: 0.1.${{ github.run_number }}
    secrets: inherit